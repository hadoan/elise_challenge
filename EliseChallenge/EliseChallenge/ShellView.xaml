<Window x:Class="EliseChallenge.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="White">

        <StackPanel HorizontalAlignment="Left" Height="100" Width="400" Margin="265,147,0,0" VerticalAlignment="Top" >
           <Slider Minimum="{Binding Minimum}" Maximum="{Binding Maximum}" Value="{Binding Value}"/>
            <Button Content="Settings" x:Name="ShowSettings" Width="75"/>
        </StackPanel>
    </Grid>

</Window>